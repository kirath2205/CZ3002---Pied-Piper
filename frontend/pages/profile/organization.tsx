import ProfileChoice from '@/components/organization/OrganizationProfileChoice';
import Layout from '@/components/shared/Layout';

export default function OrganizationProfile() {
    return (
        <Layout title='VolunteerGoWhere - Organization Profile'>
            <ProfileChoice />
        </Layout>
    );
}
